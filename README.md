## Bài nâng cao

- **Nâng cao 1:** Thêm `res/values-night/colors.xml` để hỗ trợ giao diện chế độ tối.
- **Nâng cao 3:** Tách kiểu chữ dùng lại thành `<style>` trong `themes.xml` và áp dụng cho các `TextView`.
